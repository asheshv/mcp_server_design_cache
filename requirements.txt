# Core dependencies
mcp[cli]>=1.2.0
psycopg[binary,pool]>=3.2.0
pgvector>=0.3.6
aiofiles>=24.1.0
numpy>=1.26.0

# Embedding: ONNX Runtime (lightweight, recommended for deployment)
onnxruntime>=1.18.0
tokenizers>=0.19.0

# Embedding: sentence-transformers (fallback, or for model export)
# Uncomment for development/export only -- adds ~2GB via PyTorch
# sentence-transformers>=3.0.0
