# Database Configuration
# Copy this to .env and fill in the secrets
DB_HOST=localhost
DB_READ_PASS=read_password
DB_WRITE_PASS=write_password

# Port for the MCP server (if running in non-stdio mode)
# FAST_MCP_PORT=8000
