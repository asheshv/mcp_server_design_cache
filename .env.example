# Database Configuration
# Copy this to .env and set secure passwords before running.
DB_HOST=localhost
DB_PORT=5432
DB_NAME=design_db
DB_READ_PASS=changeme_read
DB_WRITE_PASS=changeme_write

# Docker Compose -- PostgreSQL superuser password
POSTGRES_PASSWORD=changeme_super
